apiVersion: bifrost.apotema.cloud/v1alpha1
kind: Service
metadata:
  name: test-svc
spec:
  image: test-image
  port: 8080
  resources:
    limits:
      cpu: "1"
      memory: 256Mi
  kubernetes:
    namespace: default

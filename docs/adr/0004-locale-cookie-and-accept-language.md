# Locale: cookie + Accept-Language, no URL prefix in v1

Server-side locale is selected per-request as `?lang=` (override) → `lang` cookie (persisted choice) → `Accept-Language` header (browser default, q-weighted match against `SUPPORTED_LOCALES`) → `source_locale` (fallback); when `?lang=` is seen the server also `Set-Cookie`s the value (`Path=/; Max-Age=31536000; SameSite=Lax`), so a single canonical mechanism (`?lang=`) both renders-with and persists the user's choice — no `/set-locale` endpoint is needed. URL-prefix locale routing (`/es/about` vs `/en/about`) was deferred from v1 because it requires reshaping the route tree, multiplying generated routes, and adding a redirect layer that's a separate v2 design conversation; this v1 minimum makes hydration correct under SSR (server-picked locale travels to the client via the data island, so client-side `<I18nProvider>` matches the server render) without precluding URL-prefix locale routing later.

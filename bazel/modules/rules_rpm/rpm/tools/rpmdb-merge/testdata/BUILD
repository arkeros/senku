package(default_visibility = ["//rpm/tools/rpmdb-merge:__pkg__"])

# RPM general-header bytes for tzdata, produced by rpm-extract on the
# fixture in //rpm/tools/rpm-extract/testdata:tzdata.rpm. Refresh by
# rerunning rpm-extract if the upstream rpm bumps.
exports_files(["tzdata.header.blob"])

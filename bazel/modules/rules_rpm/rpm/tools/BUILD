package(default_visibility = ["//visibility:public"])

# Top-level aliases so the rules can reference tools at stable labels
# regardless of the internal directory split.
alias(name = "pin",         actual = "//rpm/tools/pin:pin")
alias(name = "rpm-extract", actual = "//rpm/tools/rpm-extract:rpm-extract")
alias(name = "rpmdb-merge", actual = "//rpm/tools/rpmdb-merge:rpmdb-merge")

package(default_visibility = ["//visibility:public"])

# Top-level aliases so the rules reference tools at stable labels
# regardless of the internal directory split. Same shape as @rules_rpm.
alias(name = "pin",          actual = "//apk/tools/pin:pin")
alias(name = "apk-extract",  actual = "//apk/tools/apk-extract:apk-extract")
alias(name = "apkdb-merge",  actual = "//apk/tools/apkdb-merge:apkdb-merge")
